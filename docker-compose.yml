services:
  app:
    build: .
    container_name: parse_probability_app
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
      - DB_URL=sqlite:///app/database.db
    env_file:
      - .env
    command: python app/app.py
